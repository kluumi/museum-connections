# Signaling server WebSocket URL (optional - defaults to production Railway server)
# VITE_SIGNALING_URL=wss://museum-connections-production.up.railway.app

# For local development with local signaling server:
# VITE_SIGNALING_URL=ws://localhost:8080
